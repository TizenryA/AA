# 2026-03-01

- 备份到 GitHub：已在 workspace 初始化 git（branch=master），remote=git@github.com:TizenryA/AA.git。
- 已生成专用 SSH deploy key：/home/node/.ssh/id_ed25519_openclaw_backup（pub 已发给哥哥用于添加到 repo deploy keys，需勾选 write）。
- 备份脚本 scripts/backup_memory_to_github.sh 已改为固定使用该 SSH key（GIT_SSH_COMMAND）。
- 当前 push 失败原因：GitHub Permission denied (publickey) —— 等待 repo 添加 deploy key 或授权。
